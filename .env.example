# =====================================================
# Variables de Entorno — SUITChASE
# =====================================================
# Copia este archivo como ".env" y modifica los valores.
#   cp .env.example .env
#
# NUNCA subas el archivo .env a GitHub.
# =====================================================

# Base de datos MySQL (instalada nativamente en el servidor)
DB_USERNAME=root
DB_PASSWORD=tu_contraseña_aqui

# Puerto del backend (por defecto 8090)
SERVER_PORT=8090
